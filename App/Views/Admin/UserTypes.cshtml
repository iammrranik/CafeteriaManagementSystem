@model IEnumerable<BLL.DTOs.UserTypeDTO>

@{
    ViewData["Title"] = "User Types";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>User Types</h2>
    <a href="/Admin/CreateUserType" class="btn btn-primary">Create New Role</a>
</div>

@if (TempData["Msg"] != null)
{
    <div class="alert alert-info">@TempData["Msg"]</div>
}

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>
                    <div class="d-flex gap-2">
                        <a href="/Admin/EditUserType/@item.Id" class="btn btn-sm btn-warning">Edit</a>
                        <form action="/Admin/DeleteUserType" method="post" onsubmit="return confirm('Are you sure you want to delete this role?');">
                            <input type="hidden" name="id" value="@item.Id" />
                            <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                        </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<div>
    <a href="/Admin/Dashboard" class="btn btn-secondary">Back to Dashboard</a>
</div>
